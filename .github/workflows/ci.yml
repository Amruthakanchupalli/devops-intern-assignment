name: Node.js Docker CI

# Trigger on push to main branch
on:
  push:
    branches:
      - main

jobs:
  build-docker:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout the repo
      - name: Checkout repository
        uses: actions/checkout@v3

      # Step 2: Set up Node.js (for npm install)
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      # Step 3: Build Docker image
      - name: Build Docker image
        run: |
          docker build -t 8byte-app .

      # Step 4: Test Docker image (optional)
      - name: Run Docker container for testing
        run: |
          docker run --rm 8byte-app node -v  # just checks node runs inside container

      # Optional: Push to Docker Hub
      # Uncomment and configure if you want to push
      # - name: Log in to Docker Hub
      #   uses: docker/login-action@v2
      #   with:
      #     username: ${{ secrets.DOCKER_USERNAME }}
      #     password: ${{ secrets.DOCKER_PASSWORD }}
      #
      # - name: Push Docker image
      #   run: |
      #     docker tag 8byte-app yourdockerhubusername/8byte-app:latest
      #     docker push yourdockerhubusername/8byte-app:latest
